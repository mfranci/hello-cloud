{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "hello-cloud.serviceAccountName" . }}
  labels:
{{ include "hello-cloud.labels" . | indent 4 }}
{{- end -}}
